### demo.perfomax
ignore/skip

### will get ships positon and status data
/home/ah-munna/Desktop/CDL/perfomax/nura_client/task/ship_positions.json
these data will replace for the current dummy data's positons and status. other data for those ships will stay same.

### icon will be white and bg red in fleet overview
in fleet overview page, the icons for internet gps and all the engines, the icon itself is white color, and the circled rounded background of the icon will be the intended dynamic color

### last data updated less than 5 minute
on the tooltip, the "last data recieved" will all be between 1-5 minutes ago.

### when map updates it shouldn't repositon
in fleet overview page, when the map update loading is happen, it repositions the screen. we can't do that. it makes moving around the map difficult.

### wind map vector. wind speeds
better windy map
this part is large. we have to implement better windy map. the previous project had good windy map. they told me to follow that one.
 /home/ah-munna/Desktop/CDL/perfomax/perfomax-maintenance-ui/pages/components/fleet-overview/index.tsx
this task is large, so we move it to phase two of our implementation.

### real time data = operation overview
alarm monitoring = real time data
this part is quite large also
we will have to rename these page paths and restructure code to point redirects to correct way.

### speed vs consumption chart and time series, less than current time
in the `/real-time-data` (that needs to be updated to `/operation-overview`) on the two charts, the data will only show until for current singapor time. if it's 2PM, in the charts, the dummy data after 2PM not needs to be shown.

### single engine speed meter names...
moved to phase two

### time date value sorting in table
in the same alarm table in alarm monitoring (real time data after update), the columns, `Date` `Time` `Value` needs sorting option. remove the sorting for engines

### machinary overview same chart color
in the machinary overview page, inside every cards, there are rows with charts. all charts color will be text-foreground or same color as the texts on the rows.

### one genset removed from machinary
in the machinery overview cards, on the second row for gensets, remove one from the list and rename the title from "Gen Set #" to "Genset #"

### condition monitoring all engine removed
in the helium header's dropdown, the first dropdown item "All Engine" needs to be removed. if it was globally all engine selected when coming to this page, it needs to be updated to the first dropdown item. but if already any specific engine were selected before coming to this page, it will stay that way.

### scatter bigger dropdown vs bigger dropdown
in the second chart, scatter `dropdown` vs `dropdown`, the dropdowns needs to be wider about 30%. and the legends shown in the chart needs to be dynamic from the two selected dropdown

### header dropdown bigger too for voyager
in `/machinery/condition-monitoring`, `/machinery/machinery-overview`, in the header's dropdown selection, the vessel dropdown needs to be wider. same size as in the `/alarm-monitoring` page's header.

### me1, me2, ae1 green in fleet overview tooltip
in the fleet overview page's tooltip for vessels, update the demo data so:
internet, gps, me1, me2, ae1, these five appear green. 
me3 and ae2 appears red color.